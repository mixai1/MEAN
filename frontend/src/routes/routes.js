import React from 'react';
import {Switch} from 'react-router-dom';

export const Routes = (isAuth) => {
    if(isAuth){
        return (
            <Switch>
                
            </Switch>
        )
    }
}   
